import { ScrollToTop } from './scroll-to-top';

describe('ScrollToTop', () => {
  it('should create an instance', () => {
    const directive = new ScrollToTop();
    expect(directive).toBeTruthy();
  });
});
